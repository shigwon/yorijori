<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.linky.api.admin.repository.AdminRepository">

    <select id="findByEmailAndPassword" resultType="com.linky.api.admin.entity.Admin">
        SELECT id, email, password, name
        FROM Admins
        WHERE email = #{email}
          AND password = #{password}
    </select>

    <select id="getOrderCountByPeriod" resultType="int">
        SELECT COUNT(*)
        FROM orders
        WHERE start_time >= #{startDateTime}
          AND start_time &lt;= #{endDateTime}
    </select>

    <select id="getHourlyOrderCountInDay" resultType="map">
        SELECT
            HOUR(start_time) as hour,
            COUNT(*) as count
        FROM orders
        WHERE start_time >= #{startDateTime}
          AND start_time &lt;= #{endDateTime}
        GROUP BY HOUR(start_time)
        ORDER BY hour
    </select>

    <select id="getDailyOrderCountInWeek" resultType="map">
        SELECT
            DAYOFWEEK(start_time) as dayOfWeek,
            COUNT(*) as count
        FROM orders
        WHERE start_time >= #{startDateTime}
          AND start_time &lt;= #{endDateTime}
        GROUP BY DAYOFWEEK(start_time)
        ORDER BY dayOfWeek
    </select>

</mapper>